import { interfaces } from "inversify";
import { TYPES } from "src/config/types";







export default class FactoryModel{


    created(T : Object){

      let obj=  Object.getOwnPropertyNames(T);
            

    }

    
}